# Timesheet — HCBB Dungeon Finder

| Date | Duration | Work done |
|---|---|---|
| 2026-07-13 | ~3h (in progress) | Bootstrap (env survey, BR/NFR, architecture, UX prompt) + design import + full v0.1.0 implementation (core, comm, matcher, session, UI, locales ×5, tests 26 green, luacheck clean, CI) + participation gate R23 (debuff "Hardcore - Boss Blitz" spellId 93131, confirmed in-game) + load-time gate via LoadOnDemand loader addon + UI fidelity pass (gradients, role icons, flow tabs, strata fix, status strip, proposal lock) + flat-tint fix (client ignores SetGradientAlpha) + custom close button + level brackets correction (unlock-1) + lead crown icon + browser click-to-invite/suggest/whisper (R24) + auto-close in combat + disable search while grouped + native player menu (suggest invite) + class name/color in browser (optional HELLO field) |
