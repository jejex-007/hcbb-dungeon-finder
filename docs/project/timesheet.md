# Timesheet — HCBB Dungeon Finder

| Date | Duration | Work done |
|---|---|---|
| 2026-07-13 | ~3.5h | Bootstrap (env survey, BR/NFR, architecture, UX prompt) + design import + full v0.1.0 implementation (core, comm, matcher, session, UI, locales ×5, tests green, luacheck clean, CI) + participation gate R23 (debuff "Hardcore - Boss Blitz" spellId 93131, confirmed in-game) + load-time gate via LoadOnDemand loader addon + UI fidelity pass (gradients, role icons, flow tabs, strata fix, status strip, proposal lock) + flat-tint fix (client ignores SetGradientAlpha) + custom close button + level brackets correction (unlock-1) + lead crown icon + browser click actions (R24) |
| 2026-07-14 | ~2h | Native player menu (right-click, suggest invite) + class name/color in browser (optional HELLO field) + auto-close in combat + disable search while grouped + countdown-bar text fix + full doc review + privacy audit + first public GitHub push (jejex-007/hcbb-dungeon-finder, CI green) + hardened diagnostic slash commands (drop /hcbb debug + /hcbb auras, cap /hcbb log at 50) |
| 2026-07-14 | ~0.25h | Backlog grooming: added M6 (post-v0.1.0 enhancements) — version negotiation + mismatch notice (NFR-C5), and per-game-mode class/role model (CoA 21 classes + Support vs Warcraft Reborn 9 base classes, no Support). Corrected mode terminology; recorded the two-mode fact in project memory. |
| 2026-07-14 | ~0.3h | Renamed the broadcast channel to `HCBBDungeonFinder` (collision hardening, decided before v0.1.0) across code + docs; explained the channel-collision test protocol (10-channel limit, name-squat check). Clarified two domain facts (HCBB is a permanent mode not a season; Support is an official CoA-defined role) and corrected project memory. Pushed (CI green), deployed copy synced. |
| 2026-07-14 | ~2h | Implemented M6.1 (version-mismatch update notice, `Codec.isNewer` + 4 tests) and M6.2 (per-game-mode role model: `NS.gameMode` from `GetRealmName`, `Data.MODE_ROLES`, Support dropped in Warcraft Reborn, cards re-space). Iterated in-game: fixed realm detection (full realm string → substring match), refactored to mode-as-source-of-truth. UI polish: clickable checkbox labels, class tooltip in the proposal popup, demo fidelity (mode-aware demo + queue-exit on demo formation). Docs (BR/NFR/architecture/README) + tracking trio. 4 commits pushed, CI green. |
| 2026-07-14 | ~0.5h | Distribution: one-click download for non-Git players — `download/HCBBDungeonFinder-latest.zip` + direct README link; rewrote `package.ps1` to build a standard forward-slash zip (worked around PS 5.1 Compress-Archive/ZipFile backslash bug) and refresh the bundle on every run. Set up the Discord server hub (name/icon/description/structure). Published the first GitHub Release **v0.1.0-beta** (`--prerelease`, zip asset); stable v0.1.0 gated on the 2-client smoke test. |
| 2026-07-14 | ~0.4h | Update-notice UX: surfaced the version-mismatch notice as an accent banner in the status strip (not just chat), `ST_UPDATE` in all 5 locales. Built a version-bumped test build + verified in-game with a 0.0.9 client seeing a 0.1.0 peer (learned: `GetAddOnMetadata` version is cached at client launch, a `/reload` doesn't refresh it — needs a full restart). Refreshed the download bundle. |
| 2026-07-15 | ~1.5h | **Who's Playing tab (R25)** + **pre-release v0.2.0**. Designed against BR/NFR first: no proto bump (a new type is additive; `HCBB2` would make 0.1.x reject even HELLO), NFR-S3 amended for class+professions, ping 120 s + jitter, ChatThrottleLib BULK. Pinned the profession model with a throwaway `/hcbb skills` dump on both realms before coding — which refuted my `isAbandonable` proposal (Ascension's custom Woodcutting/Woodworking are abandonable too) and exposed the `Herbalism`→`Herb Gathering` trap that would have silently dropped herbalism. Built `Presence` (own store + ping), `Codec` type `W` (+4 tests, 37 total), the tab, opt-out, 11 professions × 5 locales. **Found and fixed a critical latent bug**: AceEvent keeps one callback per (message, object), so modules sharing `NS.addon` silently overwrote each other — reactive matching had been dead for months (only 91 s/181 s timers), PAUSED never resumed. Surfaced only because the maintainer's "2 min, still nothing" report didn't add up. Docs (R25, NFR-S3/A2/P2, architecture §3/§4/§5/§8, README, smoke-test) + memory. 2 commits, CI green, release announced automatically in Discord (workflow's first real use) + forum copy. |
| 2026-07-15 | ~0.5h | Beta launch: wrote and posted the announcement (Ascension forums + community Discords), framed around the community challenge ending 2026-07-15 while the mode itself stays — the addon exists to keep the Blitz population findable past the event. Handled the **first user bug report**: the roles hint and the "select at least one role" error shared an anchor and rendered on top of each other (both modes, present since the start); fixed with `setRoleError()` mutual exclusion, regression pinned in the smoke-test checklist since UI layout can't be unit-tested. CI green, zip refreshed. |
| 2026-07-16 | ~1h | Wrote the Discord `#tutorial` guide (7 messages: install + the restart caveat, Find Group, matching rules, both player lists, Options, troubleshooting) around the 5 screenshots, plus the channel topic. Writing it audited the code and caught two things: the right-click menu is the **game's native player menu**, not the custom one I described — `Browser.showUnitMenu` is only a fallback if `FriendsFrame_ShowDropdown` is missing; and the freshness dots were undocumented (the author himself asked what the coloured square meant, which settles whether players needed it). **R25 then validated in the wild**: real beta players appeared in Who's Playing, one was whispered and answered, then logged off and was watched green → red → evicted at the TTL — proving cross-client comm, the `W` type, the sweep timer, and that `Presence` receives its `HCBB_CHANNEL_UP` (so the AceEvent fix holds for at least one module). Smoke-test validation log + backlog narrowed to what actually remains: the matchmaking path. |
| 2026-07-16 | ~1h | Three long-standing bugs, all found by hand during the tutorial validation, none by a test. **Dungeon filter label** never followed the selection (the only `SetText` lives in `refreshTexts`, which runs at startup and language switch only) — present since M4, invisible because nobody had clicked that filter for real. **Search blocked with no explanation** at dead levels: `[unlock-4, unlock-1]` leaves 28, 41, 42 and 60 with no eligible boss (computed from `Data.lua`); reported from a level-28 character. The hint now names the next level that opens, and level 60 gets a gold "the Blitz is over" instead of a red error (maintainer's correction: reaching 60 is the win, not a failure). Wiring it exposed a latent bug — `updateInfo` blanked `infoText` on every pool event, so the "already grouped" hint silently vanished; both paths now read one `blockedHint()`. **The first name was public** in `codec_spec.lua` as `\195\169` escapes since the first push: the mandated pre-push grep only matches plain text, and my fixed version then crashed on the regex and printed "clean" anyway. Both traps in memory with the working command. 4 commits, CI green, zip refreshed, deployed and validated in-game. Also learned the hard way: **Ascension bans for multiboxing**, so the 2-client gate needs two real people — the checklist said "two accounts are ideal" and would have walked a contributor into a hardcore ban. |
| 2026-07-16 | ~1.5h | Two maintainer-requested behaviours + the debt one surfaced, all validated in-game. **R27 cleared-by-level**: a boss past its unlock level is auto-ticked and locked (you had to kill it to get there); pure `Data:IsClearedByLevel` on the raw unlock value. **R26 fresh-only matching**: only green listings (< 60 s) are matchable, so a quiet-but-not-expired client is never proposed; `CountBracket` now returns (fresh, total) and the Find-Group line reads "N active (M total)" when some are stale — confirmed in-game showing "6 active (8 total)" during a live search. **M7 closed**: `FRESH_YELLOW` 120→90 gives the red dot a real 30 s window (browser colour only; matching/counter use `FRESH_GREEN`, eviction stays 120 s); `/hcbb demo` seeds a yellow + red stale listing to validate solo. Designed but **deferred multi-dungeon selection (M8)** — it changes the wire and the matcher, so it needs an explicit go on the wire approach first. Docs (R17/R26/R27, smoke-test, M7 ticket). 2 commits + bundle refresh, CI green. |
| 2026-07-17 | ~2.5h | **M8 multi-dungeon (R28) + update banner, v0.3.0.** Full `design ON` pass first: challenged the M7-era scoping (found the single-target wire encodes multi lists with byte-identical fallback for singles — the "invisibility" cost only hits multi listings), walked the 6-player mixed-version scenario, added the election-electorate amendment (leader must see the match; `ver` already rides every HELLO), settled presence stays 120 s (60 s buys nothing during cohabitation, 30 s doubles channel load forever). Implementation: Codec list field + canonical-ascending validation (byte-compat test pins the 0.2.0 string), `findForSelfMulti` (size-major/boss-minor iteration = the priority rule), per-boss `firstSeen`, heartbeat pruning on level-up, multi-select picker (toggle, stays open), browser "+N" + tooltip, AceDB migration, 4 keys ×5 locales, 10 new tests (47). Then the update banner: dedicated pulsing bar (AnimationGroup, no OnUpdate) + copyable direct-zip link with the full-restart caveat in the popup, freeing the status strip the old notice confiscated. Docs: R28, R1 superseded note, archi §4/§5/§6, smoke-test ×3 cases. All validated in-game solo; 2-client cases pinned. CI green. |
| 2026-07-14 | ~1h | Discord integration. In-game: **Community & support** section in the Options tab (Join our Discord + Report a Bug buttons, shared copyable-link popup `UI.CopyPopup`, `Data.LINKS`, 5 locale keys, `OKAY` whitelisted, README + architecture), copyable popup validated in-game. Automation: `discord-release.yml` GitHub Action auto-posts a rich release-announcement embed to `#announcements` (via `DISCORD_WEBHOOK_URL` secret, replacing GitHub's native webhook), verified end-to-end. Set up the Discord server (`#announcements` read-only + pinned message, `#bug-report` permissions), drafted launch/promo announcement copy. CI green. |
