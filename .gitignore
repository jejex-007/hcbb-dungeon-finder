# Local tooling — never commit (leaks machine paths/permissions)
.claude/
.vscode/
.idea/

# Build artifacts
*.zip
dist/
# ...except the ready-to-use download bundle for players without Git
!download/*.zip

# OS noise
Thumbs.db
.DS_Store
